= form_for @category do |f|
  = render 'shared/errors', obj: @category

  .form-group
    = f.label :name, class: 'form-label'
    = f.text_field :name, class: 'form-control'
  .form-group
    = f.submit 'Create Category', class: 'btn btn-primary text-light'