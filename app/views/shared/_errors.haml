- if obj.errors.any?
  .alert.alert-danger
    %h2= "#{pluralize(obj.errors.count, "error")} prohibited this article from being saved:"
    %ul
      - obj.errors.full_messages.each do |message|
        %li= message