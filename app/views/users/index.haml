
%h1.h1 Users
%ul.list-group
  - @users.each do |user|
    %li.list-group-item
      = gravatar_for user

      = link_to user.username, user_url(user)

      .small.text-muted
        = pluralize(user.articles.count, "article")

= will_paginate